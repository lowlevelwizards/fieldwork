import { AIV2Action } from "./action.js";
import { ACTION_CHANNELS } from "./action-channels.js";
const distance=(a,b)=>Math.hypot((a?.x??0)-(b?.x??0),(a?.y??0)-(b?.y??0));
export class CircumventContactAction extends AIV2Action{
  constructor({actorId,directive}={}){super({type:"CircumventContact",actorId,purpose:directive?.reason??"Create safe separation from another team",channels:[ACTION_CHANNELS.LOCOMOTION,ACTION_CHANNELS.ATTENTION],primary:true,displayPriority:88,priority:760,metadata:{directive:{...directive},provenance:directive?.provenance??null}});this.directive={...directive,destination:{...directive.destination},focus:{...directive.focus}};this.initialDistance=Math.max(1,Number(directive.initialDistance)||1);}
  canStart({game}={}){const a=game?.actors?.find(x=>x.id===this.actorId);return Boolean(a&&!a.medical?.dead&&!a.medical?.unconscious&&this.directive.destination);}
  canContinue({game}={}){const a=game?.actors?.find(x=>x.id===this.actorId);return Boolean(a&&!a.medical?.dead&&!a.medical?.unconscious&&this.directive.destination);}
  start(now,{game}={}){super.start(now);const a=game?.actors?.find(x=>x.id===this.actorId);if(a){a.currentTask=this.directive.mode==="contest"?"Contesting local access":"Avoiding opposing team";a.currentAction="Creating team separation";a.operationPausedByEncounter=true;}}
  update(delta,{game,services}={}){const a=game?.actors?.find(x=>x.id===this.actorId);if(!a)return{status:"failed",reason:"actor_missing"};services.attention.turnToward(a,this.directive.focus,delta,{pose:this.directive.mode==="contest"?"ready":"walk",turnRate:4});const result=services.locomotion.moveToward(a,this.directive.destination,delta,{game,speedMultiplier:this.directive.mode==="contest"?.54:.72,arrivalRadius:22,task:this.directive.mode==="contest"?"Taking a contest position":"Circumventing contact",pose:this.directive.mode==="contest"?"ready":"walk"});this.progress=Math.max(0,Math.min(1,1-(result.distance??distance(a,this.directive.destination))/this.initialDistance));if(result.arrived){services.locomotion.stop(a,{pose:"ready"});a.currentAction=this.directive.mode==="contest"?"Holding contested access":"Safe separation restored";return{status:"completed",reason:"contact_separation_position_reached"};}return null;}
  onInterrupted({game}={}){const a=game?.actors?.find(x=>x.id===this.actorId);if(a)a.operationPausedByEncounter=false;}
}
