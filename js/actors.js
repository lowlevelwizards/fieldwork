function resolveFacing(move,currentFacing){if(Math.hypot(move.x,move.y)<.08)return currentFacing;if(Math.abs(move.x)>Math.abs(move.y))return move.x<0?'left':'right';return move.y<0?'up':'down';}
const TOMAS_ROUTE=[{type:'walk',x:555,y:520,label:'Checking route sign'},{type:'wait',duration:3.2,label:'Reading route notice'},{type:'walk',x:1060,y:500,label:'Walking the pull-off'},{type:'wait',duration:2.4,label:'Looking over the site'},{type:'walk',x:1270,y:720,label:'Checking the shed'},{type:'wait',duration:2.8,label:'Waiting by the shed'},{type:'walk',x:760,y:690,label:'Returning to the truck'},{type:'wait',duration:3,label:'Taking notes'}];
export function createActors(){return [{id:'worker_ada',type:'actor',name:'Ada Mercer',role:'Injured Maintenance Worker',x:845,y:815,width:44,height:70,groundY:849,radius:18,vx:0,vy:0,moveSpeed:44,facing:'left',walkingPhase:0,kitId:'maintenance_worker_orange',backpackLoadRatio:0,carriedItemInstanceId:null,taskIndex:0,taskTime:0,currentTask:'Injured beside the truck',seated:true,relationship:'Unknown',interactionRadius:92,priority:48,greeting:['Please—my leg.','The first-aid supplies should be nearby.'],condition:'bleeding',severity:'urgent',mobility:'limited',needs:['bandage','water'],assessed:false},{id:'worker_tomas',type:'actor',name:'Tomas Reed',role:'Route Worker',x:610,y:500,width:44,height:70,groundY:534,radius:18,vx:0,vy:0,moveSpeed:66,facing:'left',walkingPhase:0,kitId:'maintenance_worker_yellow',backpackLoadRatio:0,carriedItemInstanceId:null,taskIndex:0,taskTime:0,currentTask:'Checking route sign',seated:false,relationship:'Unknown',interactionRadius:82,priority:29,greeting:['Ada slipped by the truck.','Please help her before anything else.','The radio is dead too.']}];}
function advance(a){a.taskIndex=(a.taskIndex+1)%TOMAS_ROUTE.length;a.taskTime=0;a.seated=false;}
export function updateActors(game,delta){const player=game.operator;for(const actor of game.actors){if(actor.operationId)continue;if(actor.medical?.dead||actor.medical?.unconscious||actor.beingDragged){actor.vx=0;actor.vy=0;actor.groundY=actor.y+actor.radius;continue;}if(actor.id==='worker_ada'){if(game.incident.bandageUsed&&game.incident.waterUsed){actor.condition='recovering';actor.severity='stable';actor.currentTask=game.incident.workerSheltered?'Recovering at the break table':'Stabilized; needs shelter';}if(game.assistedActorId!==actor.id){actor.vx=0;actor.vy=0;}actor.groundY=actor.y+actor.radius;continue;}const task=TOMAS_ROUTE[actor.taskIndex];actor.currentTask=task.label;actor.taskTime+=delta;actor.vx=0;actor.vy=0;if(task.type==='walk'){const dx=task.x-actor.x,dy=task.y-actor.y,d=Math.hypot(dx,dy);if(d<5)advance(actor);else{const sp=Math.min(actor.moveSpeed,d/Math.max(delta,.001));actor.vx=dx/d*sp;actor.vy=dy/d*sp;actor.x+=actor.vx*delta;actor.y+=actor.vy*delta;actor.facing=resolveFacing({x:actor.vx,y:actor.vy},actor.facing);actor.walkingPhase+=delta*8;}}else if(actor.taskTime>=task.duration)advance(actor);if(Math.hypot(player.x-actor.x,player.y-actor.y)<105&&task.type!=='walk')actor.facing=resolveFacing({x:player.x-actor.x,y:player.y-actor.y},actor.facing);actor.groundY=actor.y+actor.radius;}}
